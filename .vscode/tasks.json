{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-cli-debug",
      "type": "shell",
      "command": "arch -arm64 cmake -B build -DCMAKE_BUILD_TYPE=Debug -DCMAKE_OSX_ARCHITECTURES=arm64 && arch -arm64 cmake --build build -t llama-cli -j $(sysctl -n hw.logicalcpu)",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": []
    }
  ]
}
